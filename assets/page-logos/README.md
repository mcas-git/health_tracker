# Page logos

Each page has its own folder. To replace its built-in background illustration, add an SVG named
`watermark.svg` to the appropriate folder. The app detects it automatically.

Keep SVG backgrounds transparent. Simple single-colour line artwork works best with the low-opacity
watermark treatment.
